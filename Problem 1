// This program  shows the total ticket sales after each game

// Which consists of four types:

// Box, Sideline, Premium, General Admission. 

#include <iostream>

#include <conio.h>

#include <string>

using namespace std;

int main ()
{
	int Box, Sideline, Premium, General_Admission;

	int BoxTOT, SidelineTOT, PremiumTOT, General_AdmissionTOT; 
	
	double BoxTOTA, SidelineTOTA, PremiumTOTA, General_AdmissionTOTA;
	string First;

		Box = 250;
		Sideline = 100;
		Premium = 50;
		General_Admission = 25;

		Total = (Box*BoxTOTA) + (Sideline*SidelineTOTA) + (Premium*PremiumTOTA) + (General_Admission*General_AdmissionTOTA);

		
		cout << "List of Ticket Type and Ticket Price" << endl;
		cout << "Box = " << Box << endl;
		cout << "Sideline = " << Sideline << endl;
		cout << "Premium = " << Premium << endl;
		cout << "General Admission = " << General_Admission << endl;

		

		 
		
		_getch ();
		return 0;

}
